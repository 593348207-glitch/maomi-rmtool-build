# GitHub Actions 编译

把本目录完整上传到一个仓库根目录，进入 Actions，运行 `Build RMTool DEB`。

成功后下载产物 `RMTool-DEB-iOS15`：

- `RMTool-rootless.deb`：Dopamine 等 rootless 越狱。
- `RMTool-rootful.deb`：传统 rootful 越狱。

iPhone 13 / iOS 15.6.1 通常优先安装 rootless 包，具体取决于越狱类型。
